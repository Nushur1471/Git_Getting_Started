git config --global
git config --global user.name "nameGoesHere"
git config --global user.email "emailHere"
git config --global list
man git ~~~~~~~~~~~~~~~~~~~~~~~
git help
git init
git status
git remote add origin "http..............."
git add . / git add file.extension file.extension file.ex
git commit -m "message"
git push / git push [upstream_branch] [local_branch]
git status -s / git status -short
git diff --staged
git diff --staged --no-renames
git log / git log -[number of commits] / git log --oneline / git log --stat(little detailed) / git log --patch (detailed) // We can filter by author,date etc etc
git rm
git branch -r // remote branches or -a for remote and local branches
git rm // completely removes file
git rm --cached [filename] // untracks file
git mv oldfileName newFilename // renaming file ,, similliar to removing and adding tracking
git checkout -b [new_branch_name]
git checkout [branch_name] or git switch
git branch -a or -r
git clone "url"
git stash // in case of switching branch while there are files that are staged but uncommitted
		//switch branch make changes,track or stage or git add but don't commit and try to switch back to another branch
git stash -u //includes even untracked files
git stash list
git stash show
git stash clear
git stash pop
git stash apply
git marge [the branch to be marged]
git reset....

		//git visualizer
https://git-school.github.io/visualizing-git/
